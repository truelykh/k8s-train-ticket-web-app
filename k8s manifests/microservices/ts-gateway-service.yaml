apiVersion: apps/v1
kind: Deployment
metadata:
  name: ts-gateway-service
  namespace: train-ticket
  labels:
    app: ts-gateway-service
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: ts-gateway-service
  template:
    metadata:
      labels:
        app: ts-gateway-service
    spec:
      serviceAccountName: train-ticket-sa
      affinity:
        nodeAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              preference:
                matchExpressions:
                  - key: workload
                    operator: In
                    values:
                      - wnode2
                      - wnode3
                      - wnode4
      containers:
        - name: ts-gateway-service
          image: nexus-svc.nexus.svc.cluster.local:8082/truelykh/ts-gateway-service:1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 18888
              name: http
          env:
            - name: NACOS_ADDRS
              value: "nacos:8848"
            - name: SPRING_PROFILES_ACTIVE
              value: "docker"
            - name: JAVA_OPTS
              value: "-Xms64m -Xmx160m -XX:+UseSerialGC -XX:+TieredCompilation -XX:TieredStopAtLevel=1 -XX:+ExitOnOutOfMemoryError"
          resources:
            requests:
              cpu: 40m
              memory: 160Mi
            limits:
              cpu: 200m
              memory: 240Mi
---
apiVersion: v1
kind: Service
metadata:
  name: ts-gateway-service
  namespace: train-ticket
  labels:
    app: ts-gateway-service
spec:
  type: NodePort
  selector:
    app: ts-gateway-service
  ports:
    - name: http
      port: 18888
      targetPort: 18888
